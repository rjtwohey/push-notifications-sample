import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'push-notifications-sample',
  webDir: 'www'
};

export default config;
